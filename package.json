{
  "name": "ecs-logger",
  "version": "1.1.0",
  "description": "Lightweight wrapper for winston to provide ecs type hinting",
  "main": "./dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -r dist; tsc",
    "lint": "tslint -c tslint.json src/**/*.ts",
    "prepublish": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/foobarfarm/ecs-logger.git"
  },
  "keywords": [
    "logger",
    "ecs",
    "elastic",
    "type",
    "winston",
    "typescript"
  ],
  "author": {
    "name": "foobarfarm"
  },
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/foobarfarm/ecs-logger/issues"
  },
  "homepage": "https://github.com/foobarfarm/ecs-logger#readme",
  "dependencies": {
    "elastic-ecs": "^8.4.0-e",
    "typescript": "^4.9.3",
    "winston": "^3.8.2"
  },
  "files": [
    "./dist/*"
  ],
  "typings": "./dist/index.d.ts",
  "devDependencies": {
    "@types/node": "^18.11.9",
    "prettier": "^2.7.1"
  }
}
